
23/04/2026,24/04/2026
	
	-- made basic server to upload file to supabase and exposes api nodes using flask
	-- made the worker agent to get job from supabase (jobs are represent in table)
	-- the worker agent will render using blender cli and upload the output file
	-- basic html frontend to get file from user and send post request to server api

=== the basic demo version is ready
=== this version is not good enough to continue development 

-- discontinued the basic version --- 

29/04/2026

	-- made a proper system design, database schema, designed work flow( diaram available in ./artifacts)
	-- writing the worker agent work flow, and partial coding


1/05/2026

	-- wrote the code base of server to receive the job, divide the job into chunks
	-- wrote the code base of agent to get the sub task and then render
	-- every agent send back the code to the storage after job done

2/05/2026

	-- wrote the code base of merger to wait for all subtasks to complete and merge the subtasks
	-- after merging the output file uploaded to the storage
	-- all the intermideate stuff get clean and job is done

=== the agent code contains informations such as supabase key,password, redis key etc...
=== so have to change it to work with time limited tokens generated by the server
=== have to handle the node failure and job splitting depending on active nodes